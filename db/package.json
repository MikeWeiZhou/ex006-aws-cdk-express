{
  "name": "express-api-ref-db",
  "version": "0.1.0",
  "scripts": {
    "db": "docker-compose up db",
    "db-migrate": "docker-compose up --build --force-recreate db-migrate",
    "clean": "docker-compose down --rmi local",
    "destroy": "docker-compose down --rmi local -v"
  },
  "devDependencies": {
    "db-migrate": "^0.11.12",
    "db-migrate-mysql": "^2.1.2"
  }
}
